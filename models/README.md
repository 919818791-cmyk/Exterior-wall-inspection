# Model Weights

> **Paused as of 2026-07-14:** the YOLO weights deployment described below is not part of the current development, deployment, or acceptance scope. The paths are retained only for historical reference or a possible future restart. The active detection direction is the Qwen3-VL-based Trial flow.

Put production model weights in this directory on the deployment server.

Expected default paths for the trial experience:

```text
models/wall_crack_yolo11x.pt
models/missing.pt
```

Model weight files are intentionally ignored by git.
