# Model Weights

Put local model weights in this directory after cloning the repository.

Expected default paths for the trial experience:

```text
models/wall_crack_yolo11x.pt
models/missing.pt
```

The `.pt` files are intentionally ignored by git. Copy them manually or manage them outside the repository.
