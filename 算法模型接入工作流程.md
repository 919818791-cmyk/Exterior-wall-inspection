# 算法模型接入工作流程

本文档用于梳理正式环境接入真实外墙缺陷检测模型的完整流程，并标明当前仓库已经完成的准备工作。

## 1. 当前已完成

- `/trial` 简易 AI 检测体验已先行接入真实模型服务，当前范围限定为：
  - `crack` 裂缝：`models/wall_crack_yolo11x.pt`
  - `missing` 面砖剥落：`models/missing.pt`
- `algorithm-model` 已实现真实 `/predict` 推理接口，支持上传单张图片并返回 bbox、confidence、模型类型和图片尺寸。
- `algorithm-model` `/ready` 已能检查两份权重，当前验收口径是 `ready: true` 且 `crack`、`missing` 均存在。
- 后端 `/api/trial/generate` 已接入模型服务：
  - 从已上传的 quick detection 照片读取 MinIO 对象。
  - 调用 `TRIAL_ALGORITHM_INFERENCE_URL=/predict`。
  - 返回真实 findings，并携带 bbox、confidence、image_width、image_height。
- 前端 `/trial` 已收敛为“裂缝、面砖剥落”两类，结果表支持多检测框和无检出状态，并按真实 bbox 叠框。
- 当前 `.env` 使用严格推理模式：
  - `TRIAL_ALGORITHM_INFERENCE_URL=http://localhost:9004`
  - `TRIAL_INFERENCE_REQUIRED=true`
- 已保留后端 Worker API 契约：
  - `GET /api/algorithm/tasks/next`
  - `POST /api/algorithm/tasks/{task_id}/heartbeat`
  - `POST /api/algorithm/tasks/{task_id}/results`
  - `POST /api/algorithm/tasks/{task_id}/failed`
- 已实现模拟 Worker：`algorithm-worker/mock_worker.py`，可领取任务、下载图片、回传固定 JSON。
- 已在 `docker-compose.yml` 中保留 `algorithm-worker` profile。
- 已新增 `algorithm-model` 模型容器骨架：
  - `algorithm-model/Dockerfile`
  - `algorithm-model/requirements.txt`
  - `algorithm-model/server.py`
  - `algorithm-model/verify_environment.py`
- 已配置 PyTorch / Ultralytics 模型环境：
  - 默认使用 `PYTORCH_INDEX_URL=https://download.pytorch.org/whl/cu126`
  - 安装 `torch`、`torchvision`
  - 安装 `ultralytics-opencv-headless`
  - 提供 `/health`、`/ready`、`/metadata` 和 `/predict` 接口
- 已在 `docker-compose.yml` 中新增 `algorithm-gpu` profile。
- 已配置 GPU reservation：
  - `driver: nvidia`
  - `count: ${ALGORITHM_GPU_COUNT:-1}`
  - `capabilities: [gpu]`
- 已预留模型权重挂载目录：
  - 默认宿主机目录：`./models`
  - 裂缝模型容器路径：`/models/wall_crack_yolo11x.pt`
  - 面砖剥落模型容器路径：`/models/missing.pt`
- 已将模型权重目录加入 `.gitignore`，避免 `.pt` 等大文件进入仓库。
- 已更新 `.env.example`：
  - `PYTORCH_INDEX_URL`
  - `ALGORITHM_GPU_COUNT`
  - `MODEL_DEVICE`
  - `MODEL_DIR`
  - `CRACK_MODEL_WEIGHTS_PATH`
  - `MISSING_MODEL_WEIGHTS_PATH`
  - `TRIAL_ALGORITHM_INFERENCE_URL`
  - `TRIAL_INFERENCE_REQUIRED`
  - `WORKER_CONTAINER_BACKEND_BASE_URL`
- 已更新 `README.md` 和 `正式环境部署指南.md`，补充 PyTorch / CUDA 验证和模型容器启动命令。
- 已完成静态校验：
  - `docker compose config`
  - `docker compose --profile worker --profile algorithm-gpu config`
  - `python3 -m py_compile algorithm-model/server.py algorithm-model/verify_environment.py`
  - `git diff --check`

## 2. 还未完成

- 项目管理里的正式检测任务流还没有切到真实模型；`algorithm-worker` 仍主要用于模拟任务流或后续真实任务适配。
- 审核工作台和正式报告流程仍保留四类枚举兼容，但当前真实模型只验收 `crack`、`missing`。
- 还没有引入 `spalling` 剥落、`moisture` 潮湿对应权重。
- 当前机器 Compose GPU profile 报 `could not select device driver "nvidia"`，说明 NVIDIA Docker runtime 尚未就绪；目前用 CPU 容器方式跑通模型服务。
- 尚未做大批量图片、耗时、显存/内存占用和并发压测。
- 尚未基于标注样例评估置信度阈值、误检漏检和 bbox 质量。
- 尚未把真实推理结果接入项目审核工作台的端到端验收。

## 3. 需要算法同事提供

### 3.1 模型权重

当前 trial 简易版已使用：

```text
models/wall_crack_yolo11x.pt
models/missing.pt
```

如果模型文件名或路径不同，需要同步修改 `.env`：

```env
MODEL_DIR=./models
CRACK_MODEL_WEIGHTS_PATH=/models/wall_crack_yolo11x.pt
MISSING_MODEL_WEIGHTS_PATH=/models/missing.pt
```

### 3.2 模型环境版本

需要明确：

```text
Python 版本
PyTorch 版本
CUDA 版本
Ultralytics 版本
是否需要额外 pip 依赖
是否需要自定义算子
```

如果算法侧确认不是 CUDA 12.6，需要修改：

```env
PYTORCH_INDEX_URL=https://download.pytorch.org/whl/cu126
```

例如 CPU 验证可改为：

```env
PYTORCH_INDEX_URL=https://download.pytorch.org/whl/cpu
```

### 3.3 类别映射

系统后端枚举当前保留以下缺陷类型；trial 简易版只启用前两类：

| 系统枚举 | 中文含义 |
| --- | --- |
| `crack` | 裂缝 |
| `missing` | 面砖剥落 |
| `spalling` | 剥落 |
| `moisture` | 潮湿 |

算法侧需要提供模型输出类别 ID / 类别名到上述系统枚举的映射关系。

### 3.4 推理参数

需要明确：

```text
imgsz
conf
iou
device
batch
是否使用 half / fp16
是否需要按模型分别设置参数
```

### 3.5 输入输出约定

需要明确：

```text
输入是否为原图路径
是否需要先裁剪、缩放、旋转或增强
输出坐标是否基于原图尺寸
输出是 bbox、polygon 还是 mask
置信度范围是否为 0-1
是否输出严重程度 severity
```

当前后端主要落库 bbox。如果算法输出分割 mask 或 polygon，需要评估是否扩展后端、前端和审核页面。

### 3.6 测试样例

建议至少提供：

```text
3-5 张可见光外墙图片
1-2 张潮湿或面砖剥落相关图片，如适用
每张图片的期望检测结果
模型正常输出样例 JSON
```

## 4. 正式服务器准备

### 4.1 验证 GPU

在正式服务器执行：

```bash
nvidia-smi
docker run --rm --gpus all nvidia/cuda:12.9.0-base-ubuntu22.04 nvidia-smi
```

如果这两条命令失败，需要先处理 NVIDIA 驱动、Docker 或 NVIDIA Container Toolkit。

### 4.2 放置模型权重

```bash
cd /opt/building-exterior
mkdir -p models
# 将权重上传或复制到：
# models/wall_crack_yolo11x.pt
# models/missing.pt
```

### 4.3 构建模型环境

```bash
docker compose --profile algorithm-gpu build algorithm-model
```

### 4.4 验证 PyTorch / CUDA / Ultralytics

```bash
docker compose --profile algorithm-gpu run --rm algorithm-model python verify_environment.py
```

期望看到：

```text
cuda_available: true
cuda_device_count >= 1
ultralytics_version 有值
model_weights_exists: true
```

如果 `cuda_available` 为 `false`，优先检查宿主机 GPU、NVIDIA Container Toolkit 和 Compose GPU 配置。

### 4.5 启动模型容器

```bash
docker compose --profile algorithm-gpu up -d algorithm-model
docker compose --profile algorithm-gpu exec algorithm-model python verify_environment.py
```

简易体验阶段将容器端口 `9002` 映射到宿主机 `9004`，后端通过 `http://localhost:9004/predict` 调用模型服务。

## 5. 代码接入步骤

### 5.1 在 `algorithm-model` 中实现真实推理接口

建议接口：

```http
POST /predict
```

建议请求体：

```json
{
  "photo_id": "uuid",
  "image_path": "/tmp/input.jpg",
  "models": ["crack", "spalling"],
  "high_precision": false
}
```

也可以由 Worker 直接传二进制图片，具体方式由算法侧和工程侧确认。

建议响应体：

```json
{
  "photo_id": "uuid",
  "detections": [
    {
      "type": "crack",
      "type_name": "裂缝",
      "confidence": 0.91,
      "bbox": {
        "x": 120,
        "y": 80,
        "width": 260,
        "height": 140
      },
      "severity": "medium",
      "description": "疑似裂缝"
    }
  ]
}
```

### 5.2 将 `algorithm-worker` 从 mock 改为真实模式

Worker 需要完成：

1. 调用后端 API 领取任务。
2. 下载任务图片。
3. 调用 `algorithm-model` 推理接口。
4. 将模型输出转换为后端结果格式。
5. 定期发送 heartbeat，避免任务租约过期。
6. 成功时回传 results。
7. 失败时回传 failed，并附带失败原因。

### 5.3 后端结果格式

后端当前接收格式为：

```json
{
  "task_id": "uuid",
  "project_id": "uuid",
  "model_version": "facade-yolo-v1.0.0",
  "started_at": "2026-07-03T10:00:00Z",
  "finished_at": "2026-07-03T10:01:00Z",
  "results": [
    {
      "photo_id": "uuid",
      "detections": [
        {
          "id": "det-1",
          "type": "crack",
          "type_name": "裂缝",
          "confidence": 0.91,
          "bbox": {
            "x": 120,
            "y": 80,
            "width": 260,
            "height": 140
          },
          "mask": null,
          "severity": "medium",
          "description": "疑似裂缝"
        }
      ]
    }
  ]
}
```

注意：

- `type` 必须是系统支持的缺陷枚举。
- `confidence` 范围必须是 `0-1`。
- `bbox.x`、`bbox.y`、`bbox.width`、`bbox.height` 必须基于原图坐标。
- `width` 和 `height` 必须大于 0。

## 6. 端到端验收流程

### 6.1 系统基础检查

```bash
curl http://127.0.0.1:8000/api/health
docker compose ps
docker compose --profile algorithm-gpu ps
```

### 6.2 模型容器检查

```bash
docker compose --profile algorithm-gpu exec algorithm-model python verify_environment.py
```

### 6.3 业务流程检查

1. 登录系统。
2. 创建项目。
3. 上传外墙图片。
4. 配置 AI 检测模型。
5. 点击开始 AI 检测。
6. Worker 领取任务。
7. Worker 调用模型推理。
8. Worker 回传检测结果。
9. 项目状态从 `draft` 变为 `detecting`，最终变为 `pending_review`。
10. 审核工作台能看到 AI 检测框。
11. 结果详情和报告流程正常。

### 6.4 数据正确性检查

检查内容：

```text
缺陷类别是否正确
缺陷框位置是否正确
置信度是否合理
同一张图是否重复写入
空结果是否能正常流转
模型异常是否能回传失败原因
长任务是否持续 heartbeat
```

## 7. 常见问题

### 7.1 `cuda_available` 为 `false`

优先检查：

```bash
nvidia-smi
docker run --rm --gpus all nvidia/cuda:12.9.0-base-ubuntu22.04 nvidia-smi
docker compose --profile algorithm-gpu config
```

确认 Compose 中存在：

```yaml
deploy:
  resources:
    reservations:
      devices:
        - driver: nvidia
          count: 1
          capabilities: [gpu]
```

### 7.2 OpenCV 报 `libGL` 错误

正式环境应使用：

```bash
pip install ultralytics-opencv-headless
```

不要混装 `opencv-python` 和 `opencv-python-headless`。

### 7.3 Worker 容器访问不到后端

容器运行的 Worker 默认使用：

```env
WORKER_CONTAINER_BACKEND_BASE_URL=http://host.docker.internal
```

Worker 程序会自动拼接 `/api`。正式环境通过宿主机 Nginx 反向代理到后端。

如果直接绕过 Nginx 访问后端，需要确保后端监听地址不是仅 `127.0.0.1`，否则 Docker 容器无法访问宿主机回环地址。

### 7.4 Worker 下载不到 MinIO 图片

检查 `.env`：

```env
MINIO_ENDPOINT=服务器IP:9002
MINIO_PUBLIC_URL=http://服务器IP:9002
```

不要在正式环境保留 `localhost:9002`，否则容器或其他电脑无法访问预签名 URL。

### 7.5 模型执行时间超过任务租约

当前默认：

```env
WORKER_LEASE_SECONDS=600
```

如果单个任务图片多或模型较慢，需要：

- Worker 在每张图或每个批次后发送 heartbeat。
- 必要时调大 `WORKER_LEASE_SECONDS`。
- 避免一次性处理超大 batch 导致长时间无心跳。

## 8. 建议交付顺序

1. 算法同事提供模型权重、类别映射、推理参数、测试样例。
2. 在正式服务器验证 GPU 和 Docker GPU 运行时。
3. 构建 `algorithm-model`。
4. 运行 `verify_environment.py`。
5. 实现 `algorithm-model /predict`。
6. 改造 `algorithm-worker` 调用真实模型。
7. 使用测试图片做离线推理验证。
8. 使用系统真实任务做端到端联调。
9. 完成审核页、结果页、报告链路验收。
10. 固化模型版本号和部署文档。
